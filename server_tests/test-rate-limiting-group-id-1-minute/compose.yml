x-test-name: &test-name test-rate-limiting-group-id-1-minute
x-test-token: &test-token AIK_RUNTIME_1_test-rate-limiting-group-id-1-minute
x-app-service: &app-service app-test-rate-limiting-group-id-1-minute
x-database-name: &database-name test-rate-limiting-group-id-1-minute

services:
  setup-test-rate-limiting-group-id-1-minute:
    extends:
      file: ../compose.templates.${COMPOSE_OS:?Set COMPOSE_OS}.yml
      service: setup-template
    environment:
      TEST_NAME: *test-name
      TEST_TOKEN: *test-token

  app-test-rate-limiting-group-id-1-minute:
    extends:
      file: ../compose.templates.${COMPOSE_OS:?Set COMPOSE_OS}.yml
      service: app-template
    env_file:
      - ./test.env
    environment:
      AIKIDO_TOKEN: *test-token
      DATABASE_NAME: *database-name
      DATABASE_URL: '${DATABASE_URL_PREFIX:?Set DATABASE_URL_PREFIX}test-rate-limiting-group-id-1-minute${DATABASE_URL_SUFFIX:?Set DATABASE_URL_SUFFIX}'
    depends_on:
      setup-test-rate-limiting-group-id-1-minute:
        condition: service_completed_successfully

  test-rate-limiting-group-id-1-minute:
    extends:
      file: ../compose.templates.${COMPOSE_OS:?Set COMPOSE_OS}.yml
      service: test-template
    environment:
      TEST_NAME: *test-name
      TEST_TOKEN: *test-token
      TEST_SERVER_HOST: *app-service
      TEST_CONTROL_SERVER_HOST: *app-service
    depends_on:
      app-test-rate-limiting-group-id-1-minute:
        condition: service_started
