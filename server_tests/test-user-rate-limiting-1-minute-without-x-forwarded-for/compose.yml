services:
  test-user-rate-limiting-1-minute-without-x-forwarded-for:
    profiles:
      - policies
    extends:
      file: ../compose.templates.${COMPOSE_OS:?Set COMPOSE_OS}.yml
      service: app-template
    env_file:
      - ./test.env
    environment:
      AIKIDO_TOKEN: AIK_RUNTIME_1_test-user-rate-limiting-1-minute-without-x-forwarded-for
      DATABASE_NAME: test-user-rate-limiting-1-minute-without-x-forwarded-for
      DATABASE_URL:
        '${DATABASE_URL_PREFIX:?Set
        DATABASE_URL_PREFIX}test-user-rate-limiting-1-minute-without-x-forwarded-for${DATABASE_URL_SUFFIX:?Set
        DATABASE_URL_SUFFIX}'
