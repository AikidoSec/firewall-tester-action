x-test-name: &test-name test-user-rate-limiting-1-minute-enable-disable
x-test-token: &test-token AIK_RUNTIME_1_test-user-rate-limiting-1-minute-enable-disable
x-app-service: &app-service app-test-user-rate-limiting-1-minute-enable-disable
x-database-name: &database-name test-user-rate-limiting-1-minute-enable-disable

services:
  setup-test-user-rate-limiting-1-minute-enable-disable:
    extends:
      file: ../compose.templates.${COMPOSE_OS:?Set COMPOSE_OS}.yml
      service: setup-template
    environment:
      TEST_NAME: *test-name
      TEST_TOKEN: *test-token

  app-test-user-rate-limiting-1-minute-enable-disable:
    extends:
      file: ../compose.templates.${COMPOSE_OS:?Set COMPOSE_OS}.yml
      service: app-template
    env_file:
      - ./test.env
    environment:
      AIKIDO_TOKEN: *test-token
      DATABASE_NAME: *database-name
      DATABASE_URL: '${DATABASE_URL_PREFIX:?Set DATABASE_URL_PREFIX}test-user-rate-limiting-1-minute-enable-disable${DATABASE_URL_SUFFIX:?Set DATABASE_URL_SUFFIX}'
    depends_on:
      setup-test-user-rate-limiting-1-minute-enable-disable:
        condition: service_completed_successfully

  test-user-rate-limiting-1-minute-enable-disable:
    extends:
      file: ../compose.templates.${COMPOSE_OS:?Set COMPOSE_OS}.yml
      service: test-template
    environment:
      TEST_NAME: *test-name
      TEST_TOKEN: *test-token
      TEST_SERVER_HOST: *app-service
      TEST_CONTROL_SERVER_HOST: *app-service
    depends_on:
      app-test-user-rate-limiting-1-minute-enable-disable:
        condition: service_started
