x-test-name: &test-name test-allow-traffic-by-countries
x-test-token: &test-token AIK_RUNTIME_1_test-allow-traffic-by-countries
x-app-service: &app-service app-test-allow-traffic-by-countries
x-database-name: &database-name test-allow-traffic-by-countries

services:
  setup-test-allow-traffic-by-countries:
    extends:
      file: ../compose.templates.${COMPOSE_OS:?Set COMPOSE_OS}.yml
      service: setup-template
    environment:
      TEST_NAME: *test-name
      TEST_TOKEN: *test-token

  app-test-allow-traffic-by-countries:
    extends:
      file: ../compose.templates.${COMPOSE_OS:?Set COMPOSE_OS}.yml
      service: app-template
    env_file:
      - ./test.env
    environment:
      AIKIDO_TOKEN: *test-token
      DATABASE_NAME: *database-name
      DATABASE_URL: '${DATABASE_URL_PREFIX:?Set DATABASE_URL_PREFIX}test-allow-traffic-by-countries${DATABASE_URL_SUFFIX:?Set DATABASE_URL_SUFFIX}'
    depends_on:
      setup-test-allow-traffic-by-countries:
        condition: service_completed_successfully

  test-allow-traffic-by-countries:
    extends:
      file: ../compose.templates.${COMPOSE_OS:?Set COMPOSE_OS}.yml
      service: test-template
    environment:
      TEST_NAME: *test-name
      TEST_TOKEN: *test-token
      TEST_SERVER_HOST: *app-service
      TEST_CONTROL_SERVER_HOST: *app-service
    depends_on:
      app-test-allow-traffic-by-countries:
        condition: service_started
