services:
  control-test-update-aikido:
    extends:
      file: ../compose.templates.${COMPOSE_OS:?Set COMPOSE_OS}.yml
      service: control-app-template
    env_file:
      - ./test.env
    environment:
      AIKIDO_TOKEN: AIK_RUNTIME_1_control-test-update-aikido
      DATABASE_NAME: control-test-update-aikido
      DATABASE_URL:
        '${DATABASE_URL_PREFIX:?Set
        DATABASE_URL_PREFIX}control-test-update-aikido${DATABASE_URL_SUFFIX:?Set
        DATABASE_URL_SUFFIX}'
